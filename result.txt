Python计算字符串或者文件md5值使用hashlib模块，hashlib主要提供字符串加密的功能，支持md5,sha1, sha224, sha256, sha384, sha512等算法‬‫‫‪‬‫‫‪。这里我们只用到md5‬‫‬‬‬‭‪‬。
‪‭‬‪‫‪‫‪原来在python2中还支持md5模块，但这个模块在python 2.5以后就已经不再推荐‫‬‬‭‬‭‪‪。python3中被废弃‫‫‭‪‫‬‪‪。‫‬‭‭‭‪‭‬